name: Hello CI

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Mostrar ruta actual
        run: pwd

      - name: Listar archivos
        run: ls -la

      - name: Mensaje simple
        run: echo "CI corriendo"
